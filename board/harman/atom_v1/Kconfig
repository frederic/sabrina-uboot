if TARGET_MESON_TXLX

config SYS_CPU
	string
	default "armv8"

config SYS_BOARD
	string
	default "atom_v1"

config SYS_VENDOR
	string
	default "harman"

config SYS_SOC
	string
	default "txlx"

config SYS_CONFIG_NAME
	default "atom_v1"

endif
